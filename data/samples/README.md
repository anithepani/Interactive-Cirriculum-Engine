# Sample tutorials

Small clips + descriptors for Phase-0 smoke tests. Keep media out of git (the
`.gitignore` excludes `*.mp4`); commit only `.md` descriptors and short transcripts.

## Sample 1: Python Dictionaries (matches data/fixtures/transcript_sample.json)

- Source: YouTube (replace with a real, short, permissively-licensed clip)
- Duration: ~2 min
- Concepts: dict create, get, keys/values/items
- Used by: `scripts/run_pipeline.py` smoke test + `eval/golden_set/G1_*`
